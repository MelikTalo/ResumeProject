
@{
    Layout = "~/Views/Shared/_DefaultLayout.cshtml";
}

    <!-- banner section -->
    @Html.Action("PartialFeature")
    <!-- //banner section -->
    <!-- About Section-->
    @Html.Action("PartialAbout")
    <!-- //About Section-->
    <!-- Services Section -->
    @Html.Action("PartialService")
    <!-- //Services Section -->
    @Html.Action("PartialTecnology")
    <!-- Why Choose Us Section-->
    @Html.Action("PartialSkill")
    <!-- //Why Choose Us Section-->
    <!-- Stats Section-->
    @Html.Action("PartialCounter")
    <!-- //Stats Section-->
    <!-- portfolio -->
    @Html.Action("PartialProject")
    <!-- //portfolio -->
    <!--/brands-->
    @Html.Action("PartialBrands")
    <!-- //brands -->
   